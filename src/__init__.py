"""
Representation Overlap Lab - source package.

This package provides the data loading, embedding, similarity, projection,
overlap scoring, and visualization modules for the lab. All heavy computation
runs offline via the scripts/ pipeline. The modules here are used both by
those scripts and by the Streamlit app at runtime.
"""
